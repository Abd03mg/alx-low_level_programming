#include <stdio.h>

/**
* main - Print all single digit numbers
*
* Return: Always 0 (Success)
*/
int main(void)
{
	int nums = 0;
	while(nums < 10)
    {
        printf("%d",nums);
        nums++;
	putchar('\n')
    }
	return 0;
}
