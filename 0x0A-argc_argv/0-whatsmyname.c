#include "main.h"

/**
 * main - program that print its name.
 * @argc: number of arguments.
 * @argv: program comilintation argument.
 * Return: 0 if success.
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("/s\n", *argv);
	return (0);
}
